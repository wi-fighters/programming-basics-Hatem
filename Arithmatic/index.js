// 1. Calculate the percentage

    const firstNumber= 17 ;
    const secondNumber = 30 ;
    const percentage = (firstNumber * 100) / secondNumber ;
    console.log(`percentage = ${percentage}%`);

// 2. Working with Averages 

    const noemyAverage = (8+6+5+9+8+2+1+8.5+7+4)/10;
    console.log(`noemyAverage =${noemyAverage}`);

// 3. Score

    const score = (85 * 6) - (75+70+85+90+100);
    console.log("Score in the sixth test: "+ score);

//4. The Minimum Score

    let minimumScore = [(0.8 * 9)*100] - [(0.78 * 8) * 100];
    console.log(` James needs a minimum of ${minimumScore}% to get an 80% average`);

//   5. Consumer Formula 

    let bill1 = 22.35 , bill2 = 26.67 , bill3 = 35.92;
    let bill1withTip =bill1+ (22.35 * 10)/100; 
     let bill2withTip =bill2+ (26.67 * 15)/100;
     let bill3withTip =bill3+ (35.92 * 20)/100;
    console.log(`bill1 with tip: ${bill1withTip}
     bill2 with tip: ${bill2withTip}
     bill3 with tip: ${bill3withTip}`);

// 6. Geometry Formulas

    let perimeterofaSquare = 4 * 4.75;
    console.log(`the perimeter of a square: ${perimeterofaSquare}cm`);
// Perimeter of a Triangle

    let perimeterofaTriangle = 5 + 6 + 7;
    console.log(`the perimeter of a triangle: ${perimeterofaTriangle}cm`);

// Area of a Square

let areaofaSquare = 5 * 5;
console.log(`the area of a square: ${areaofaSquare}`);

// Area of a Triangle

    let s = 5 + 6 + 7 ;
    let p = s / 2 ;
    let areaofaTriangle = Math.sqrt(p*(p-5)*(p-6)*(p-7));
    console.log(`the area of a triangle: ${areaofaTriangle}`); 

// Volume of a Cube

    const volumeofaCube = 9 * 9 * 9 ;
    console.log(`the volume of a cube: ${volumeofaCube}`);

//  7. Basic Word Problems
//  At the movies

    const numberofPeople = 6450 / 15 ;
    console.log(`the number of people : ${numberofPeople}`);

// Income per Week

    const income = 500 * 52 ;
    console.log(`the income per year : ${income}$`);


